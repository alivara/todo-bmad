# --- BMAD tooling (installed framework + skills) ---
# Not committed: each developer installs BMAD themselves. We track deliverables only.
.claude/
_bmad/
.bmad-loop/

# --- Personal / machine-local overrides ---
**/settings.local.json
**/*.user.toml
**/*.user.yaml

# --- BMAD ephemeral / working artifacts (not deliverables) ---
**/.memlog.md
_bmad-output/party-mode/
**/.working/

# --- OS / editor cruft ---
.DS_Store

# --- Runtime env (never commit real secrets; .env.example IS tracked) ---
.env
**/.env
!**/.env.example

# --- web (Next.js / Node) ---
web/node_modules/
web/.next/
web/out/
web/next-env.d.ts
web/coverage/
web/playwright-report/
web/test-results/
web/*.tsbuildinfo

# --- api (Go) ---
api/bin/
api/tmp/
*.test

# Generated axe-core scan dumps (regenerated by the a11y e2e lane each run; the human report accessibility-audit.md stays tracked)
_bmad-output/test-artifacts/a11y/*.json
